Stop the plan iterator loop.

## Instructions

1. Remove the loop marker:
   ```bash
   rm -f .claude/loop
   ```

2. Confirm to the user: "Plan iterator stopped. Resume later with `touch .claude/loop` (plan stays in `docs/plan.md`)."
